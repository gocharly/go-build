# Обновление стандарта GO-BUILD: v1.10.0 — Agent Behavioral Discipline & Strict Scope Adherence

**Дата**: 2026-09-09  
**Версия**: v1.10.0  
**Приоритет**: CRITICAL  
**Статус**: Активен  

---

## 1. Причина обновления

В работе AI-ассистентов часто проявляется деструктивный паттерн «самовольного расширения» (Scope Creep):
- Агент самовольно добавляет в интерфейс тексты, кнопки, поля, блоки, уведомления, статусы и страницы, о которых пользователь не просил;
- Добавляются элементы «для удобства», «для красоты», «на будущее» или «по стандарту»;
- Пишется избыточная fallback-логика, дублирующие проверки «на всякий случай» и обработка гипотетических ошибок;
- Рефакторятся несвязанные файлы и меняется архитектура за пределами целевой задачи;
- Создаются ненужные абстракции, временный код и неиспользуемые импорты.

Сформулирован фундаментальный стандарт поведения: **Агент не имеет права самовольно добавлять тексты, UI-элементы, проверки, состояния или другую функциональность, которой не было в запросе.**

---

## 2. Новые правила стандарта (Раздел 7 в SKILL.md)

### 9. Строгое соответствие запросу
- Агент реализует только то, что явно запросил пользователь.
- Запрещено самостоятельно добавлять тексты, кнопки, поля, блоки, страницы, уведомления, статусы, элементы интерфейса или другую функциональность, которая не была запрошена.
- Запрещено добавлять элементы «для удобства», «для красоты», «на будущее» или «по стандарту», если пользователь отдельно этого не просил.
- Запрещено изменять существующий текст без прямой необходимости для выполнения запроса.
- Если пользователь просит изменить конкретный элемент, остальные элементы должны оставаться без изменений.
- Не добавлять дополнительные сценарии, состояния, fallback-механизмы или альтернативные варианты поведения без необходимости.
- Не добавлять дополнительные проверки только потому, что они потенциально могут быть полезны.
- Не добавлять комментарии, логи, debug-информацию или сообщения пользователю, если они не нужны для выполнения задачи.
- Не переименовывать существующие элементы, файлы, функции, переменные или компоненты без необходимости.
- Не менять архитектуру проекта шире области, которую затрагивает запрос.
- Не добавлять зависимости, библиотеки или инструменты, если задача может быть выполнена без них.
- Любое дополнительное изменение должно быть непосредственно необходимо для выполнения запрошенной задачи.
- Если изменение не требуется для результата — оно не выполняется.

### 10. Модульность
- Повторно используемую или логически самостоятельную функциональность выносить в отдельный модуль/пакет.
- Модуль должен иметь одну понятную ответственность.
- Внешний код взаимодействует с модулем через его публичный API.
- Повторное использование выполняется через `import`, а не через копирование кода.
- Основные файлы не должны содержать внутреннюю реализацию вынесенных модулей.
- Не создавать отдельный модуль только ради формального разделения файлов.
- Если функциональность используется только в одном месте и не имеет самостоятельной ответственности, не выносить её без необходимости.
- Модуль должен быть максимально независимым от остального проекта.
- Изменение внутренней реализации модуля не должно требовать изменений в коде, который использует его публичный API.

### 11. Очистка кода
- Удалять неиспользуемый код.
- Удалять неиспользуемые импорты.
- Удалять неиспользуемые зависимости.
- Удалять мёртвые функции, переменные, компоненты и файлы.
- Не оставлять старую реализацию после замены новой.
- Не оставлять временный код после завершения задачи.
- Не оставлять debug-код и временные логи после завершения отладки.
- Не дублировать существующую функциональность.
- После изменения проверять затронутые файлы на оставшийся мусор.

### 12. Минимум проверок
- Добавлять только проверки, необходимые для корректной работы программы.
- Не добавлять проверки «на всякий случай».
- Не дублировать одну и ту же проверку на нескольких уровнях без необходимости.
- Не добавлять fallback-логику без реального сценария, который она обрабатывает.
- Не добавлять обработку гипотетических ошибок, которые не относятся к текущей задаче.
- Не усложнять код дополнительными условиями, если тот же результат достигается более простым способом.
- Каждая проверка должна иметь конкретную причину существования.
- Если проверка не влияет на корректность, безопасность или выполнение реального сценария — её следует удалить.

### 13. Принцип минимального изменения
- Изменять только необходимые файлы.
- Изменять только необходимый код.
- Не выполнять рефакторинг несвязанных частей проекта.
- Не менять существующее поведение, которое пользователь не просил менять.
- Не улучшать код за пределами текущей задачи без прямого запроса.
- После выполнения задачи проект должен содержать только необходимые для неё изменения.

### 14. Запрет самовольного расширения
Агент не должен самостоятельно придумывать:
- дополнительные функции;
- дополнительные UI-элементы;
- дополнительные тексты;
- дополнительные настройки;
- дополнительные проверки;
- дополнительные состояния;
- дополнительные страницы;
- дополнительные API;
- дополнительные зависимости;
- дополнительные абстракции;
- дополнительные файлы;
- дополнительные сценарии использования.

Если пользователь этого не запросил и это не требуется технически для выполнения задачи — этого не должно быть в результате.

---

## 3. Измененные файлы

1. `SKILL.md`:
   - Внедрен Раздел 7 «Жёсткие правила поведения агента и запрет самовольного расширения» сразу после 6 базовых столпов.
   - Разделы 8-18 сквозным образом перенумерованы.
   - Операционные правила дополнены пунктом 3 (Strict Scope & No Scope Creep).
   - Чек-лист раздела 16 дополнен блоком «6. Agent Behavioral & Scope Check».
   - Версия стандарта повышена до `v1.10.0`.
2. `GEMINI.md` и `.agents/AGENTS.md`:
   - Ключевые требования дополнены пунктами 9-14 на системном уровне.
3. `DESIGN_MEMORY.md`:
   - В Avoid Rules добавлено правило `Avoid: Scope Creep & Unauthorized Expansion`.
4. `templates/subagent-team-blueprint.md`:
   - Инструкции субагентов дополнены требованиями Strict Scope, Minimal Checks и Minimal Changes.
   - Валидация расширена до 6 чек-листов (включая Agent Behavioral & Scope Check).
5. `CHANGELOG.md`:
   - Зафиксирован релиз `v1.10.0`.

---

## 4. Study / Review
**Priority**: CRITICAL
- [x] Интегрировать правила 9-14 в `SKILL.md` сразу после 6 столпов.
- [x] Синхронизировать глобальные правила `GEMINI.md` и `AGENTS.md`.
- [x] Проверить все роли субагентов на наличие запрета самовольного расширения.
- [ ] Разработать процедуру сверки diff перед каждым коммитом/ответом на предмет отсутствия лишних файлов и правок вне скоупа.
